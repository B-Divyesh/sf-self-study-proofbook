[
  {
    "id": "privacy-local",
    "claim": "Your study entries stay in this browser unless you export them.",
    "where": "landing facts, boundaries, privacy page, README",
    "test": "npm test -- --grep @claim:privacy-local",
    "sandbox": "fresh browser context on /demo; edit and save; assert every request remains same-origin"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "landing facts, README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context on /demo; wait for the service worker, reload once, go offline, and reload"
  },
  {
    "id": "csv-export",
    "claim": "CSV export contains one row per attempt.",
    "where": "app archive tools, landing archive section, README",
    "test": "npm test -- --grep @claim:csv-export",
    "sandbox": "fresh /demo data; download CSV and assert one header plus three attempt rows"
  },
  {
    "id": "revision-history",
    "claim": "Each changed solution keeps the earlier saved version.",
    "where": "landing method, app revision history, README",
    "test": "npm test -- --grep @claim:revision-history",
    "sandbox": "fresh /demo data; change a solution, save, and inspect both revisions"
  },
  {
    "id": "print-index",
    "claim": "Creates a printable mastery index with sources, time, status, and revisions.",
    "where": "landing preview and method, app archive tools, README",
    "test": "npm test -- --grep @claim:print-index",
    "sandbox": "fresh /demo data; open the print route and assert three complete rows and credential disclaimer"
  },
  {
    "id": "encrypted-backup",
    "claim": "A backup uses AES-256-GCM encryption, and its password is not stored.",
    "where": "landing archive section, app archive tools, README",
    "test": "npm test -- --grep @claim:encrypted-backup",
    "sandbox": "fresh /demo data; create a backup, decrypt it with Web Crypto AES-256-GCM, and assert neither persistent storage nor the downloaded bytes retains the password"
  },
  {
    "id": "json-revisions",
    "claim": "JSON export keeps every revision.",
    "where": "app archive tools, landing archive section, README",
    "test": "npm test -- --grep @claim:json-revisions",
    "sandbox": "fresh /demo data; download JSON and assert all three sample attempts plus the two saved revisions on the analysis attempt"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo changes never enter the real proofbook.",
    "where": "demo banner, README",
    "test": "npm test -- --grep @claim:demo-isolation",
    "sandbox": "fresh context; load three demo attempts, leave demo, and assert the real ledger is empty"
  },
  {
    "id": "archive-tools-included",
    "claim": "The current release has no paid tier; JSON, CSV, printing, and password-encrypted backups are included in the local ledger.",
    "where": "landing facts, landing archive section, README purchase model",
    "test": "npm test -- --grep @claim:archive-tools-included",
    "sandbox": "fresh /demo data; assert no checkout or license UI, download JSON and CSV, create a password-encrypted backup, and open the printable index without any checkout request"
  },
  {
    "id": "cited-attempt",
    "claim": "Each attempt becomes one cited, revisable record.",
    "where": "landing hero image caption, README",
    "test": "npm test -- --grep @claim:cited-attempt",
    "sandbox": "fresh /demo data; create a cited attempt, reload, and assert its source and reference in the editor and printable index"
  },
  {
    "id": "topics-and-goals",
    "claim": "Groups problem attempts by topic and study goal.",
    "where": "README",
    "test": "npm test -- --grep @claim:topics-and-goals",
    "sandbox": "fresh /demo data; add a named topic and goal, assign a new attempt, reload, and assert both labels persist"
  },
  {
    "id": "attempt-timer",
    "claim": "Times an attempt and keeps its elapsed time in the print index and CSV export.",
    "where": "landing method, README",
    "test": "npm test -- --grep @claim:attempt-timer",
    "sandbox": "fresh /demo data with Playwright's deterministic clock; start, advance, and pause an attempt timer, reload, then assert the elapsed time in the editor, print index, and CSV"
  },
  {
    "id": "evidence-status",
    "claim": "Adds a learner-set status and confidence score.",
    "where": "README",
    "test": "npm test -- --grep @claim:evidence-status",
    "sandbox": "fresh /demo data; set status and confidence, reload, and assert both in the editor and printable index"
  },
  {
    "id": "json-complete-archive",
    "claim": "JSON is the complete archive and keeps every revision.",
    "where": "README",
    "test": "npm test -- --grep @claim:json-complete-archive",
    "sandbox": "fresh /demo data; export JSON, import it into an empty real ledger, re-export, and compare topics, attempts, sources, timers, status, confidence, and revisions"
  },
  {
    "id": "no-credential-service",
    "claim": "Proofbook records practice; it does not issue credentials.",
    "where": "landing boundaries, README, terms, printable index",
    "test": "npm test -- --grep @claim:no-credential-service",
    "sandbox": "fresh /demo data; open the printable index, assert its learner-maintained non-credential notice, and intercept all requests to confirm no credential service is contacted"
  }
]
